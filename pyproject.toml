[project]
name = "dont_touch_the_column"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.11.*"
dependencies = [
    "pygame==2.6.1",
]

[project.scripts]
dont_touch_the_column = "dont_touch_the_column:main"

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[tool.uv]
only-binary = ["pygame"]